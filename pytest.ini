[pytest]
asyncio_mode = auto
testpaths = tests
markers =
    llm: tests that call the real Claude backend (slow, costs tokens; deselected by default)
# By default, skip the slow real-LLM tests. Run them explicitly with: pytest -m llm
addopts = -m "not llm" -q
